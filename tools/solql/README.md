# solql — Placeholder

The original repository `https://github.com/0xJasonn/solql` was not available when this workspace was assembled (May 2026).

## What it was

A query language / toolkit for analyzing Solidity source at scale.

## Recommended actions if you need it

1. Search for active forks or successor projects.
2. If a working upstream appears again, run from repo root:

   ```bash
   git submodule add https://github.com/0xJasonn/solql.git tools/solql
   ```

3. Then re-run:

   ```bash
   ./scripts/setup-node-tools.sh
   ```

Until then, this directory is intentionally left as documentation only.
